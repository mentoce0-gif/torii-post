# キュレーション手順 — PoC開始前に人がやること

このPoCの信用は、施設情報が正しいことではなく、
**確認していないことを確認したと言わないこと**に乗っています。
嘘の情報1件で製品の信用は落ちます。空欄も推測も禁止です。

## 値のルール

| 値 | 意味 | 入れてよい条件 |
| --- | --- | --- |
| `○` | 利用可能と確認済み | 出典を読んだ、または現地で見た |
| `△` | 条件付き / 弱い | 同上。「一部だけ」「時期限定」など |
| `×` | 無い / 利用困難 | 同上。**「見つからなかった」は `×` ではありません** |
| `？` | 確認不能 | それ以外すべて |

`？` は失点ではありません。情報確度が下がるだけで、候補からは外れません。

### 各項目が何を意味するか

値が揺れる原因はほぼここなので、先に決めてあります。

| 項目 | 判定の対象 | 対象外 |
| --- | --- | --- |
| トイレ | **その公園に利用できるトイレが存在するか**（自治体の公衆トイレ一覧に載っていれば `○`） | 遊具からの距離・経路。これは `notes` に書きます |
| オムツ台 | 屋外または誰でも入れる場所にあるか | **併設の有料施設（体育館・プール・店舗）の中にあるもの** |
| 日陰 | 木陰・東屋・屋根付きベンチなど、退避先として明記されたもの | 「森がある」「緑地」からの推論 |
| 水道 | 手や靴を洗える水栓 | 給水スポット（マイボトル用）だけの記載 |
| ベビーカー | 園内を通行できると読める記載 | 「段差が多い」から `△` を作ること。**段差の記載は `？` の理由であって `△` の根拠ではありません** |

**トイレを「遊具から到達できるか」で判定しないでください。**
判定ルール側（`rules.ts`）は「トイレがないため長居には不向き」という
**存在**の話をしています。到達性を値に混ぜると、同じ `○` が公園ごとに
違う意味になります。到達性が気になる場合は `notes` に書いてください。

帯状の公園（大津湖岸なぎさ公園など）は**区域ごとに設備が違います**。
どの区域の話かを `notes` に必ず書き、区域が特定できない情報は `？` です。

## いま入っているもの

`src/server/data/seed/places.ts` は 13件。京都〜大津〜草津〜守山。

- **設備値**: 大津の2件を除き、`家・室内` 以外はすべて `？` です。
  この環境から施設サイトへ到達できず、出典を読めていないためです。
- **確定済み（2026-09-08）**: 皇子が丘公園（砂場 `○` / トイレ `○`、確度33%）と
  大津湖岸なぎさ公園 市民プラザ側（トイレ `○`、確度17%）。
  いずれも大津市のオープンデータ（都市公園遊具一覧・公衆トイレ一覧）が出典で、
  2つのAIによる独立した監査を通しています。
- **営業情報**: 全件 `hours_status: 'unverified'` /「利用可能時間 未確認」。
- **座標**: 市区町村の代表点（`coord_precision: 'locality'`）。
  移動時間は「（目安）」付きで表示されます。
- **出典欄**: 13件すべてに「読むべきページ」の候補URLが `checkedAt: null`＝**未確認**
  として入っています（公式サイト / 自治体ページ、計30件）。
  これは**検索で在り処を特定しただけ**で、中身は誰も読んでいません。
  設備値は1つも解錠しません。探す手間を省くためだけのものです。
  えんまどう公園と第2なぎさ公園だけは個別ページが見つからず、守山市の公園一覧を
  入口として入れてあります。
  設備が実際に載っていそうな下層ページ（`hikari-g.com/kihan/facility/`、
  `kyoto-ga.jp/kodomonorakuen/facilities/`、各市の「赤ちゃんの駅」一覧など）も
  足してあるので、トップページから辿り直す必要はありません。

**開く前に [`CURATION_LEADS.md`](./CURATION_LEADS.md) を読んでください。**
どのページに6項目のどれが載っていそうか、検索で見えた範囲の当たりが書いてあります。
併せて、検索の要約をそのまま信じると踏む罠（要約が存在を推測する / 第1と第2の
なぎさ公園が混ざる / 季節限定の設備が通年のように書かれる）も記録してあります。
**あちらの記述は証拠ではありません。値の根拠にはできません。**

`npm run dev`（`SEED_PROFILE=demo`）では、これとは別に
`demoObservations.ts` の仮データが読み込まれ、画面に
「デモデータ」バナーが出ます。**この値は事実ではありません。**
`places.ts` に昇格させず、必ず調べ直してください。

## 1件を確定させる手順

1. 公式サイト・自治体ページ・現地のいずれかで確認する。
   多くの場合、開くべきURLは `candidateSources` にもう入っています。
2. 読んだら、その出典の `checkedAt` に**読んだ日**を入れる（`null` のままにしない）。
   候補になければ `sources` を1つ足す。

   ```ts
   sources: [
     {
       key: 'enmadou-park:city',
       kind: 'municipal_page',
       label: '守山市 公園一覧',
       url: 'https://…',
       checkedAt: '2026-09-14',   // 読んだ日
     },
   ],
   ```

3. 確認できた設備だけ `equipment` に書く。書かなかったキーは自動で `？` です。

   ```ts
   equipment: {
     toilet: { value: '○', sourceKey: 'enmadou-park:city', verifiedAt: '2026-09-14', confidence: 1 },
   },
   ```

4. 営業情報が読めたら `hoursStatus` を `'documented'`、`hoursLabel` に実際の記載を。
   読めなければ触らないでください。
5. 現地で座標を取ったら `coordPrecision: 'exact'` に。移動時間から「（目安）」が消えます。
6. `npm test` を通す。出典なしで値を書くと**起動時に例外で落ちます**
   （`validateSeed`）。これは仕様です。

   テストは「値がゼロであること」ではなく「値に裏付けがあること」を見ています。
   **正しく出典を付けて値を埋めれば、テストは通ります**（緩める必要はありません）。
   落ちるのは次の3つです:

   | 落ち方 | 意味 |
   | --- | --- |
   | `validateSeed` が起動時に例外 | 出典または確認日がない |
   | `claims ○ on demo data` | `demoObservations.ts` の値を昇格させた |
   | `cites …, which nobody has read yet` | `checkedAt: null` のままの候補URLを根拠にした |

## PoC開始前チェックリスト

人にしかできないもの。**この環境からはWebそのものに到達できないため、
ここは未着手のまま残っています。**

`curl` も `WebFetch` も、施設サイトどころか `google.com` すら
egressプロキシに `403` で止められます。通るのは検索ツールだけで、
それが返すのは要約であってページ本文ではありません。
一次情報を読む行為自体が実行できないので、6項目の確定は人の作業です。
下ごしらえは [`CURATION_LEADS.md`](./CURATION_LEADS.md) に集めてあります。

- [ ] 13件それぞれについて、公開情報で確認できた設備を埋めた（残りは `？` のまま）
- [ ] 少なくとも上位候補になりやすい3〜5件は現地で6項目を確認した
- [ ] 「逃げ道」（ぐずったときにどうするか）を主要候補に書いた
- [ ] 料金表記を確認した（`未確認` のままでも可。推測は不可）
- [ ] 検証世帯に「`？` は未確認であって、無いという意味ではない」と説明した

### 機械が見るようになったもの

以前は目視だった2項目は `tests/seed.test.ts` に移しました。
`npm test` が通る限り成立しているので、毎回確認する必要はありません。

- [x] `SEED_PROFILE=poc` で起動し、デモバナーが出ないこと
      → `serves no placeholder value and no banner under SEED_PROFILE=poc`
- [x] `demoObservations.ts` の値が1つも `places.ts` に紛れていないこと
      → `declares no demo source in the curated seed`

思い出したときだけ成立する確認は、保証ではありません。
急いで seed を編集した次の人にも効くよう、テストに固定してあります。
